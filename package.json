{
  "name": "ee-stars",
  "version": "2.0.0",
  "description": "EE-Stars — Inmobiliaria Digital Perú",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "npm run install:all && concurrently --names \"API,WEB\" --prefix-colors \"yellow,cyan\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup": "npm install --prefix backend && npm install --prefix frontend && echo \"\n✅ Instalación completa. Ahora configura tu .env en backend/.env y ejecuta: npm run dev\"",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.3"
  }
}
